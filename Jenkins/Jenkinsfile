pipeline {
    agent {
       label 'slave1'
    }
    stages {
       stage ('BUILD') {
           steps {
                sh 'sleep 1'
           }
       } 
       
       stage ('TEST') {
           steps {
                sh 'sleep 1'
           }
       }
        
       stage ('DEPLOY') {
           steps {
                sh 'sleep 1'
           }
       }  
       stage ('DEPLOY1') {
           steps {
                sh 'sleep 1'
           }
       }     
    }
}
